<% @events.each do  |e|%>
  <%= e.message %>
<% end %>

<%= form_for @event do |e| %>
<%= e.hidden_field :league_id, :value => @league.id%>
<%= e.label :message %> <br />
<%= e.text_area :message, :size => "44x6" %><br />
<%= submit_tag("Post") %>
<% end %>
