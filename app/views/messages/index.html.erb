<% @messages.each do  |m|%>
  <%= m.message %>
<% end %>

<%= form_for @message, :url => {:action => :create} do |m| %>
<%= m.hidden_field :league_id, :value => @league.id %>
<%= m.label :message %> <br />
<%= m.text_area :message, :size => "44x6" %><br />
<%= submit_tag("Post") %>
<% end %>
